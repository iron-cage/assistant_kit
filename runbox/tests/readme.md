# run/tests

Regression tests for the runbox shell script infrastructure.

| File | Responsibility |
|------|----------------|
| `runbox_build_safety_test.sh` | Bug reproducers: build lock and post-build image verify (issue-runbox-build-safety). |
