# run/tests

Regression tests for the runbox shell script infrastructure.

| File | Responsibility |
|------|----------------|
| `runbox_build_safety_test.sh` | Regression tests: build lock, post-build image verify, and content hash staleness detection (T1–T4). |
