# run/tests

Regression tests for the runbox shell script infrastructure.

| File | Responsibility |
|------|----------------|
| `runbox_build_safety_test.sh` | Regression tests: build lock, post-build image verify, content hash staleness detection, OCI mountpoint pre-creation, and hash scope coverage (T1–T6). |
