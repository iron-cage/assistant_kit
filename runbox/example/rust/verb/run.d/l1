#!/usr/bin/env bash
# run.d/l1 — execute binary via cargo run directly (container execution layer).
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec cargo run --manifest-path "$SCRIPT_DIR/../../Cargo.toml"
