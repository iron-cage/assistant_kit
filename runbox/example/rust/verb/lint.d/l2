#!/usr/bin/env bash
# lint.d/l2 — run linter via runbox (host orchestration layer).
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR/../.."
if [[ "${1:-}" == "--dry-run" ]]; then echo "./run/runbox .lint"; exit 0; fi
exec ./run/runbox .lint
