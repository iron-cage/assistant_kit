#!/usr/bin/env bash
# lint.d/l1 — run cargo clippy directly (container execution layer).
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec cargo clippy --manifest-path "$SCRIPT_DIR/../../Cargo.toml" --all-targets -- -D warnings
