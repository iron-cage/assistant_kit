#!/usr/bin/env bash
# do build — build the container image via runbox
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR/.."
if [[ "${1:-}" == "--dry-run" ]]; then echo "./run/runbox .build"; exit 0; fi
exec ./run/runbox .build
