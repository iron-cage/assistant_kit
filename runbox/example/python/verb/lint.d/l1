#!/usr/bin/env bash
# lint.d/l1 — run ruff directly (container execution layer).
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec "$SCRIPT_DIR/../../.venv/bin/ruff" check --no-cache "$SCRIPT_DIR/../../src/" "$SCRIPT_DIR/../../tests/"
