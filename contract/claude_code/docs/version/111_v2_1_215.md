# Version: 2.1.215

### Scope

- **Purpose**: Records the changelog for Claude Code release 2.1.215.
- **Responsibility**: Documents all features, fixes, and changes introduced in version 2.1.215.
- **In Scope**: All changelog entries published for version 2.1.215.
- **Out of Scope**: Prior and subsequent releases (see sibling version doc instances).

### Abstract

Claude no longer runs the `/verify` and `/code-review` skills on its own — invoke them explicitly with `/verify` or `/code-review` when needed.

### Changes

- Claude no longer runs the `/verify` and `/code-review` skills on its own; invoke them with `/verify` or `/code-review` when you want them
