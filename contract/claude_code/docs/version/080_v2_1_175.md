# Version: 2.1.175

### Scope

- **Purpose**: Records the changelog for Claude Code release 2.1.175.
- **Responsibility**: Documents all features, fixes, and changes introduced in version 2.1.175.
- **In Scope**: All changelog entries published for version 2.1.175.
- **Out of Scope**: Prior and subsequent releases (see sibling version doc instances).

### Abstract

Added `enforceAvailableModels` managed setting — when enabled, the `availableModels` allowlist also constrains the De...

### Changes

- Added `enforceAvailableModels` managed setting — when enabled, the `availableModels` allowlist also constrains the Default model (a Default that would resolve to a disallowed model now falls back to the first allowed model), and user or project settings can no longer widen a managed `availableModels` list
