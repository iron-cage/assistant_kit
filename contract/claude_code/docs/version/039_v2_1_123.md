# Version: 2.1.123

### Scope

- **Purpose**: Records the changelog for Claude Code release 2.1.123.
- **Responsibility**: Documents all features, fixes, and changes introduced in version 2.1.123.
- **In Scope**: All changelog entries published for version 2.1.123.
- **Out of Scope**: Prior and subsequent releases (see sibling version doc instances).

### Abstract

Fixed OAuth authentication failing with a 401 retry loop when `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1` is set

### Changes

- Fixed OAuth authentication failing with a 401 retry loop when `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1` is set
