# run

Shell scripts for `claude_assets` container operations.

| File | Responsibility |
|------|----------------|
| `runbox` | Thin wrapper: delegates to workspace runbox-run with module config. |
| `runbox.yml` | Module Docker config: image, scope, plugins, test script path. |
