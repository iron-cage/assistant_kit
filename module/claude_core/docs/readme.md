# docs/

### Scope

**Responsibilities:** Public API contracts for the `claude_core` crate.
**In Scope:** Public API contracts (`api/`).
**Out of Scope:** Source code (→ `src/`), automated tests (→ `tests/`), build scripts (→ `verb/`).

### Responsibility Table

| File | Responsibility |
|------|----------------|
| `api/` | Public library API contracts (`settings_io` atomic JSON KV engine, `toml_io` tiered TOML KV engine) |
