# Command Groups

### Scope

- **Purpose**: Formalize sets of commands that share one implementing function and one parameter set, differing only in default values.
- **Responsibility**: Define command_group membership under a strict identity test — same handler function, same parameter set — distinct from the looser cross-command comparisons in `param_group/` and `docs/feature/037_accounts_usage_param_unification.md`.
- **In Scope**: Group membership, the Representation Absorption Test applied per candidate pair, shared-handler citations, default divergence (when any exists), and cross-references to commands/parameters/tests/user stories.
- **Out of Scope**: Individual parameter semantics (-> `../param/`), looser multi-command comparisons that don't share an identical parameter set (-> `../param_group/readme.md`, `../../feature/037_accounts_usage_param_unification.md`).

Every command in `command/` is evaluated against every other command using the Representation Absorption Test (see Evaluated, Not Qualifying below for the worked tests) before a new command name is ever added — this is a mandatory design gate, not documentation-after-the-fact. Every command belongs to exactly one group — including a command with no sibling, which still opens its own Singleton Group and still receives a row below; no command is ever left out of **All Groups**. Before adding a new command, this same evaluation also asks whether an existing group's handler could absorb it via a new parameter instead of a new dispatch function. (Per this workspace-group's shared rulebook, CLI Documentation: Command Group Total Partition and Command Group Minimization.)

### Responsibility Table

| File | Responsibility |
|------|----------------|
| readme.md | This file — group index, membership rule, and All Groups partition |

### All Groups (15 total)

| # | Group | Members | Shared Handler | Divergence |
|---|-------|---------|-----------------|------------|
| 1 | `.credentials.status` | 1 | `credentials_status_routine` (`src/commands/credentials.rs:80`) | N/A — sole member |
| 2 | `.accounts` | 1 | `accounts_routine` (`src/commands/accounts.rs:70`) | N/A — sole member |
| 3 | `.account.limits` | 1 | `account_limits_routine` (`src/commands/limits.rs:90`) | N/A — sole member |
| 4 | `.account.save` | 1 | `account_save_routine` (`src/commands/account_ops.rs:240`) | N/A — sole member |
| 5 | `.account.use` | 1 | `account_use_routine` (`src/commands/account_ops.rs:19`) | N/A — sole member |
| 6 | `.account.delete` | 1 | `account_delete_routine` (`src/commands/account_ops.rs:331`) | N/A — sole member |
| 7 | `.account.relogin` | 1 | `account_relogin_routine` (`src/commands/account_relogin.rs:25`) | N/A — sole member |
| 8 | `.account.renewal` | 1 | `account_renewal_routine` (`src/commands/account_renewal.rs:24`) | N/A — sole member |
| 9 | `.account.inspect` | 1 | `account_inspect_routine` (`src/commands/account_inspect.rs:125`) | N/A — sole member |
| 10 | `.model` | 1 | `model_routine` (`src/commands/model.rs:88`) | N/A — sole member (absorbed the former `.model.select` command's `scope::subprocess` behavior; still a Singleton — one handler, wider `scope::`-routed responsibility, see Feature 035) |
| 11 | `.models` | 1 | `models_routine` (`src/commands/models.rs:37`) | N/A — sole member |
| 12 | `.token.status` | 1 | `token_status_routine` (`src/commands/token_paths.rs:22`) | N/A — sole member |
| 13 | `.paths` | 1 | `paths_routine` (`src/commands/token_paths.rs:84`) | N/A — sole member |
| 14 | `.usage` | 1 | `usage_routine` (`src/usage/api.rs:62`) | N/A — sole member |
| 15 | `.` | 1 | `dot_routine` (`src/commands/dot.rs:18`, registered inline `src/cli.rs:48`) — registered but unreachable; see note below table | N/A — sole member |

**Total:** 15 groups for 15 commands (15 singletons) — a 1:1 groups-to-commands ratio, the maximum possible under the Minimization rule (§ CLI Documentation : Command Group Minimization); every group is a Singleton because the underlying sweep (below) found zero cross-calls among any pair of dispatch functions. `.help` and the 4 DEPRECATED/REMOVED commands documented in `docs/cli/command/001_account.md` and `docs/cli/command/007_model.md` (`.account.rotate`, `.account.assign`, `.account.unclaim`, `.model.select`) are excluded from this partition and from the ratio — see "Commands Outside the Partition" below for why.

**Note on Group 15 (`.`):** `docs/cli/command/003_meta.md:27-31` and a fresh read of `src/adapter.rs:114-118` confirm `dot_routine`'s registration is real (`src/cli.rs:48`, a genuine `Box::new(...)` call, distinct from every other command's handler) but the handler is **never invoked at runtime** — `argv_to_unilang_tokens()` rewrites bare `.` to `.help` with `needs_help=true` at Step 4 (`src/adapter.rs:115-118`), and `src/cli.rs:161-165` returns via `print_usage()` before the unilang pipeline (and therefore `dot_routine`) is ever reached. `.` remains its own Singleton Group here (it has a distinct, citable registration entry that `.help` entirely lacks — see "Commands Outside the Partition"), but readers should not infer `dot_routine`'s body is live/tested code from its presence in this table.

**Verification method (2 independent passes):**

1. **Full pairwise dispatch-function grep sweep.** All 16 dispatch/routine functions were freshly re-enumerated from their `Box::new(...)` registration call sites — `credentials_status_routine` (`src/commands/credentials.rs:80`, registered `src/registry.rs:73`), `accounts_routine` (`src/commands/accounts.rs:70`, registered `src/registry.rs:132`), `account_limits_routine` (`src/commands/limits.rs:90`, registered `src/registry.rs:133`), `account_save_routine` (`src/commands/account_ops.rs:240`, registered `src/registry.rs:142`), `account_use_routine` (`src/commands/account_ops.rs:19`, registered `src/registry.rs:164`), `account_delete_routine` (`src/commands/account_ops.rs:331`, registered `src/registry.rs:167`), `account_relogin_routine` (`src/commands/account_relogin.rs:25`, registered `src/registry.rs:168`), `account_renewal_routine` (`src/commands/account_renewal.rs:24`, registered `src/registry.rs:178`), `account_inspect_routine` (`src/commands/account_inspect.rs:125`, registered `src/registry.rs:186`), `model_routine` (`src/commands/model.rs:88`, registered `src/registry.rs:206`), `models_routine` (`src/commands/models.rs:37`, registered `src/registry.rs:213`), `model_select_routine` (`src/commands/model_select.rs:23`, registered `src/registry.rs:232`), `token_status_routine` (`src/commands/token_paths.rs:22`, registered `src/registry.rs:207`), `paths_routine` (`src/commands/token_paths.rs:84`, registered `src/registry.rs:214`), `usage_routine` (`src/usage/api.rs:62`, registered `src/registry.rs:264`), `dot_routine` (`src/commands/dot.rs:18`, registered `src/cli.rs:48`) — then every one of the 16 was grepped across the entire `src/` tree for calls to it, excluding its own `fn name(...)` definition line: `for fn in <16 names>; do grep -rn "$fn(" src/ | grep -v "fn $fn"; done`. **Result: zero cross-calls for all 16 functions**, confirming all 16 groups are Singleton Groups. The single non-empty hit across the whole sweep is a doc comment in `src/commands/credentials.rs:16` ("Called by `credentials_status_routine()` to read subscription, tier, email, display, role, and billing.") describing the private single-consumer helper `read_live_cred_meta()` — prose referencing the routine by name, not a second dispatch function calling it. This is what backs the "zero cross-calls" claim below: it is a grep-verified structural fact, not informal reasoning.
2. **End-to-end re-read of every `docs/cli/command/*.md` file** (`001_account.md` through `008_models.md`, plus `readme.md` and `procedure.md`) hunting for prose that could be misread as an implementation-sharing claim even where the strict test fails — phrases like "same pipeline," "same resolution logic," "behaves identically," "shared," "no duplication." Every such phrase found was verified against source via targeted grep/read before being classified as either a genuine dispatch-function share (none found) or a near-miss. Result: the table below carries 5 near-miss rows total — 2 from the original pass (`.accounts`/`.usage` owner-dispatch mutation sharing; `.model`/`.models`/`.model.select`) plus 3 identified in a later pass (`.accounts`/`.usage` live-fetch pipeline claim; `.model`/`.account.use`/`.usage` `set_model::`/`set::` write path; `.account.use`/`.account.inspect` expired-token refresh path).

**Post-sweep note (Feature 035):** the sweep above, and the 16-function enumeration it lists, reflect the code state at the time it ran — `.model.select` and `model_select_routine` were both still live. Feature 035 subsequently retired `.model.select` as a standalone command by direct design instruction (merged into `.model` via `scope::`), overriding — not confirming — this sweep's own "not qualifying" verdict for that specific pair (see the updated Evaluated, Not Qualifying row below). That merge is now implemented (Task 465). Correction to this note's original assumption: `model_select_routine` was not deleted, but gutted to a migration-error stub (`src/commands/model_select.rs:23`) — still registered (`src/registry.rs:232`) but excluded from the All Groups partition above (see Commands Outside the Partition) since it performs no live grouped role. A fresh, targeted cross-call check for the two functions Feature 035 actually touched (`grep -rn "model_routine(" src/` / `grep -rn "model_select_routine(" src/`, each excluding its own definition and registration lines) confirms zero cross-calls against every other routine in `src/` — the current 15-function All Groups partition remains all-Singleton. This targeted check is scoped to the functions Feature 035 changed; it is not a re-run of the full original 16-function pairwise sweep above.

This is a structural fact, not a coverage gap: every `reg_cmd()` / `register_with_routine()` call in `src/registry.rs` (and the one inline call in `src/cli.rs`) passes a **distinct** routine function — no two commands are ever wired to the same `Box::new(...)` handler. Criterion (b) of the command_group membership rule (literal same dispatch/handler function) therefore fails for every possible pair before parameter-set comparison (criterion (a)) is even relevant.

### Commands Outside the Partition

**Applicability boundary:** the Total Partition (§ CLI Documentation : Command Group Total Partition, this workspace-group's shared rulebook) requires every command with a live dispatch function to receive exactly one row above. Two categories of entries in `docs/cli/command/` have no live dispatch function to group in the first place, and are documented here rather than forced into a fabricated row:

| Entry | Status | Why excluded from All Groups |
|-------|--------|-------------------------------|
| `.help` | Live, but framework-provided | Pre-registered by the unilang `CommandRegistry::new()` itself — `claude_profile` defines no routine function for it at all. Per `docs/cli/command/003_meta.md:94`, the adapter layer (`src/cli.rs:149-162`, `needs_help` flag set in `src/adapter.rs:66`) intercepts `.help` (and bare `help`) *before* the unilang dispatch pipeline runs at all, routing to `print_usage()` (`src/cli.rs:71`) instead of any registered `CommandRoutine`. There is no `Box::new(...)` handler to cite as a "Shared Handler" — the column has nothing to point at that isn't either fabricated or a description of framework-internal, non-`claude_profile` code. |
| `.account.rotate` | DEPRECATED (Feature 038) | Zero registration hits for `account.rotate`/`account_rotate` across `src/registry.rs`, `src/cli.rs`, `src/commands/*.rs` (fresh grep, this session). Superseded entirely by the `rotate::` parameter on `.usage` (`src/registry.rs:258`). No dispatch function exists to group. |
| `.account.assign` | REMOVED (Feature 037) | Zero registration hits for `account.assign`/`account_assign`. Superseded by `.accounts assignee::`. No dispatch function exists to group. |
| `.account.unclaim` | REMOVED (Feature 037; further REMOVED Feature 064) | Zero registration hits for `account.unclaim`/`account_unclaim`. Superseded by `.accounts owner::0`. No dispatch function exists to group. |
| `.model.select` | REMOVED (Feature 035) | Merged into `.model` via the new `scope::` parameter. Kept registered only as a stub emitting a migration error (`docs/cli/command/007_model.md` Command 20), mirroring the `.account.assign`/`.account.unclaim` pattern above — no live dispatch function performing `.model.select`'s former role remains to group. |

These five entries are excluded from the 15-group/15-command ratio above, not folded into it as zero-cost padding — the ratio measures live dispatch functions under active minimization pressure, and none of these five contribute one.

### Evaluated, Not Qualifying

| Candidate Pair | Shared Implementation | Why Not a Command Group |
|-----------------|------------------------|---------------------------|
| `.accounts` / `.usage` | `owner_dispatch::owner_batch_clear()`, `owner_dispatch::owner_named_dispatch()`, `owner_dispatch::bool_field_batch_set()`, `owner_dispatch::bool_field_named_dispatch()` (`src/owner_dispatch.rs`) — called from `accounts_routine` (`src/commands/accounts.rs:282,285,302,304`) and from `usage_routine` via `handle_mutation_dispatch()` (`src/usage/api_dispatch.rs:267,270,307,310`) | Different top-level routine functions (`accounts_routine` in `src/commands/accounts.rs:70` vs. `usage_routine` in `src/usage/api.rs:62`, re-exported from `src/usage/mod.rs:35`) — fails criterion (b) regardless of parameter overlap. Parameter sets also diverge beyond defaults: `.usage` registers 3 params `.accounts` does not (`rotate::`, `who::`, `solo::` — `src/registry.rs:258,260,262`), and `.accounts` registers 14 legacy field-presence params (`current::`, `sub::`, `tier::`, `expires::`, `email::`, `display_name::`, `host::`, `role::`, `billing::`, `model::`, `uuid::`, `capabilities::`, `org_uuid::`, `org_name::`) that `.usage` never registers at all — these are dead `REMOVED_TOGGLE` stubs kept only so `.accounts` can emit a `cols::`-migration error, not live parameters (`src/registry.rs:117-130`). Fails criterion (a) as well. `docs/feature/037_accounts_usage_param_unification.md` line 19 describes these two commands as sharing "an identical parameter interface" — that phrasing is imprecise; the two commands share a large common core (confirmed at the parameter-name level in `../param_group/readme.md`'s Fetch/Sort/Display Control rows) but are not identical, and even a truly identical parameter set would not be sufficient on its own without a shared handler. See `../../feature/037_accounts_usage_param_unification.md` for the documented (looser, and now more precisely qualified) relationship. |
| `.accounts` / `.usage` (live-fetch pipeline claim) | None found — `../command/001_account.md:9,114` claims `.accounts` "fetches live quota using the same pipeline as `.usage`" when `refresh::1`/`touch::1` are set | `accounts_routine()` (`src/commands/accounts.rs:70-380`) registers `refresh::`, `touch::`, `imodel::`, and `effort::` as accepted parameters (`src/registry.rs:92-95`) but **never reads any of the four** — `grep -n "refresh\|touch\|fetch" src/commands/accounts.rs` returns zero matches in the routine body. `usage_routine()`'s live-fetch machinery (`fetch_quota_for_list()` at `src/usage/fetch.rs:63`, called only from `src/usage/api.rs:131`; `apply_refresh()` at `src/usage/refresh.rs:76`, called only from `src/usage/api.rs:141`; `apply_touch()` at `src/usage/touch.rs:133`, called from `src/usage/api.rs:152,326`) is never invoked from `accounts.rs` at all. The doc prose describes intended/aspirational parity, not implemented behavior — the four params are currently accepted-but-inert in `.accounts`. Fails criterion (b) (no shared handler — in fact, on this specific sub-claim, no shared *helper* either) more starkly than the general `.accounts`/`.usage` divergence above. |
| `.model` / `.account.use` / `.usage` (`set_model::`/model-write path) | `claude_profile_core::account::set_session_model()` (`../claude_profile_core/src/account.rs:586`) — called from `model_routine` (`src/commands/model.rs:88`), `account_use_routine` (`src/commands/account_ops.rs:149`), and `usage_routine` (`src/usage/api.rs:166`) | Three distinct dispatch functions (`model_routine` `src/commands/model.rs:88`, `account_use_routine` `src/commands/account_ops.rs:19`, `usage_routine` `src/usage/api.rs:62`) — `set_session_model()` lives in a different crate (`claude_profile_core`) entirely below the dispatch layer, alongside every other file I/O primitive these commands use; sharing a lower-layer write helper is expected code reuse, not dispatch-function identity. Parameter sets diverge sharply: `.model` has 6 params (`scope::`, `model::`, `effort_level::`, `reset_model::`, `reset_effort_level::`, `format::` — Feature 035 unification superseded this row's original 2-param/`set::` description) where every write action is routed by `scope::` against one of two backing stores; `.account.use` has 8 params (`src/registry.rs:146-166`) where `set_model::` is one of many, invoked as a side effect of switching accounts; `.usage` has 33 params (`src/registry.rs:215-264`) where `set_model::` is likewise one of many, invoked as a side effect of a quota-fetch-and-render cycle. `.model`'s `model::`/`effort_level::` and the other two commands' `set_model::` are different parameter names with different invocation semantics — not the same parameter under a different default. `docs/cli/command/007_model.md:94` states `set_session_model() is also called by set_model:: on .account.use/.usage (Feature 034) — same write primitive, distinct command/parameter, not merged into .model.` — this is an accurate description of helper-level reuse, but "same write primitive"/"distinct command/parameter" describes the write primitive, not the three commands' dispatch functions or parameter surfaces, which remain fully independent. |
| `.account.use` / `.account.inspect` (expired-token refresh path) | `crate::usage::attempt_expired_token_refresh()` (`src/usage/api_switch.rs:95`) — called from `account_use_routine` (`src/commands/account_ops.rs:215`) and `account_inspect_routine` (`src/commands/account_inspect.rs:165`); both ultimately reach `claude_profile_core::account::refresh_account_token()` (`../claude_profile_core/src/account.rs:731`) | Two distinct dispatch functions (`account_use_routine` `src/commands/account_ops.rs:19`, `account_inspect_routine` `src/commands/account_inspect.rs:125`). `docs/cli/command/001_account.md:801` (in the `.account.inspect` section) states `refresh::1 (default) behaves identically to .usage's refresh::1: calls refresh_account_token() once...` — this is imprecise in a specific, verifiable way: `.account.inspect` shares its refresh wrapper (`attempt_expired_token_refresh()`) with `.account.use`, not directly with `.usage`. `usage_routine` reaches the same bottom-layer `refresh_account_token()` through a different wrapper, `apply_refresh()` (`src/usage/refresh.rs:76`, calling `refresh_account_token()` at `src/usage/refresh.rs:119`, itself called only from `src/usage/api.rs:141`) — a separate function with separate retry/batch semantics (`apply_refresh()` iterates the full account list and applies 401/403/429 auth-error retry logic; `attempt_expired_token_refresh()` is a single-account, locally-expired-only helper with no HTTP-error branching). All three commands (`.account.use`, `.account.inspect`, `.usage`) converge only at the deepest shared primitive `refresh_account_token()`, which also has a fourth call site inside `usage_routine`'s idle-activation path (`src/usage/touch.rs:151`) — this is ordinary layered reuse of a single OAuth-refresh primitive, not evidence of any two commands sharing a dispatch function or parameter set. Parameter sets also diverge: `.account.inspect` has 4 params (`src/registry.rs:179-186`), `.account.use` has 8 (`src/registry.rs:146-166`), `.usage` has 33 (`src/registry.rs:215-264`) — no two are identical. |
| `.model` / `.models` (historically also `.model.select`) | None — no shared lower-level helper found beyond the generic `format::` output-formatting convention | **Superseded in part by Feature 035 — see note below.** At the time of this sweep, three distinct routine functions existed (`model_routine`, `models_routine`, `model_select_routine`, registered at `src/registry.rs:206,213,232` in the current source) with three non-overlapping parameter sets beyond `format::` (`.model`: `set::`; `.models`: `offline::`, `name::`; `.model.select`: `id::`, `reset::`), and different config surfaces entirely (`.model` writes `~/.claude/settings.json`, `.model.select` writes `~/.clr/config.toml`, `.models` reads a catalog/API and writes nothing) — failing both criteria for all three. **`.model`/`.model.select` was subsequently merged anyway, by direct design instruction (Feature 035), as a single CLI verb with an explicit `scope::` router over what remain two distinct backing stores internally — this is a deliberate override of this row's verdict for that one pair, not a correction of it; the underlying analysis (no shared handler, no shared parameters, different files) was and remains accurate.** `.model`/`.models` (the pair not covered by Feature 035's merge) remains not-qualifying on the original, unoverridden analysis: distinct routine functions, non-overlapping parameters beyond `format::`, and `.models` never writes any file. |

### Navigation

*(none — all 15 groups are Singleton Groups; no per-group detail file is warranted)*
