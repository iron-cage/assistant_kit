# testing/param_group/

Per-parameter-group integration test specifications for `clp` CLI.

| File | Responsibility |
|------|----------------|
| `output_control.md` | Test cases for the Output Control group (`v::`, `format::`). |
