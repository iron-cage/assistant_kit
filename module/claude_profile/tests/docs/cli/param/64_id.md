# Parameter 064: `id::` — Edge Cases

> **Narrowed scope** (Feature 035/Task 465): EC-01 through EC-10 below test `id::` as `.model.select`'s original set-mode selector against `~/.clr/config.toml`'s `model` key — that role is retired; `.model.select` now returns a migration-error stub unconditionally, regardless of `id::` (see [20_model_select.md](../command/20_model_select.md)). `id::` remains live, but exclusively as `.provider.select`'s set-mode selector against the `provider` key — current coverage: [21_provider_select.md](../command/21_provider_select.md) (IT-03, IT-04, IT-07, IT-08, IT-09, IT-12), backed by `account_provider_test.rs` (`t08_provider_select_set_kimi_persists_and_confirms`, `t10_provider_select_empty_id_exits_1`, `t11_provider_select_id_and_reset_mutually_exclusive`). See [docs/cli/param/064_id.md](../../../../docs/cli/param/064_id.md)'s own "Narrowed scope" note. Cases below are retained for historical reference only — their cited function names no longer exist in the test suite.

**Behavioral Divergence Pair:** EC-02 ↔ EC-03 — `id::VALUE` alone activates set mode and writes the preference, exit 0; `id::VALUE reset::1` together is rejected as mutually exclusive, exit 1.

### Test Case Index

| ID | Test | Scenario | Expected | Status |
|----|------|----------|----------|--------|
| EC-01 | `ec1_id_absent_defaults_to_get_mode` | `.model.select` with no `id::` | reads and prints current preference (get mode) | N/A |
| EC-02 | `ec2_id_present_activates_set_mode` | `id::claude-opus-4-8` | writes `model` to `~/.clr/config.toml`'s user tier; exit 0 | N/A |
| EC-03 | `ec3_id_and_reset_mutually_exclusive` | `id::claude-opus-4-8 reset::1` | exit 1 — `id:: and reset::1 are mutually exclusive` | N/A |
| EC-04 | `ec4_id_empty_value_rejected` | `id::` (empty value) | exit 1 — `id:: must be a non-empty model ID` | N/A |
| EC-05 | `ec5_id_min_boundary_single_char` | `id::x` (shortest non-empty value) | accepted; writes `model = "x"` verbatim; exit 0 | N/A |
| EC-06 | `ec6_id_max_boundary_long_value` | `id::` with a long realistic model ID string (e.g. `claude-haiku-4-5-20251001`) | accepted; written verbatim, no truncation; exit 0 | N/A |
| EC-07 | `ec7_id_no_live_validation_against_api` | `id::not-a-real-model-xyz` | accepted at write time — non-empty check only, no live API validation; exit 0 | N/A |
| EC-08 | `ec8_id_overwrites_existing_pin` | `~/.clr/config.toml` already has `model = "claude-sonnet-5"` → `id::claude-opus-4-8` | overwrites to `"claude-opus-4-8"`; exit 0 | N/A |
| EC-09 | `ec9_id_preserves_other_prefs_keys` | `~/.clr/config.toml` has unrelated keys alongside `model` | other keys preserved verbatim after write; exit 0 | N/A |
| EC-10 | `ec10_id_creates_prefs_file_and_dir_if_absent` | `~/.clr/` directory does not exist | `.model.select id::claude-opus-4-8` creates `~/.clr/` and `config.toml`; exit 0 | N/A |

**Total:** 10 edge case tests

---

### EC-01: `id::` absent — get mode reads current preference

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** `~/.clr/config.toml` contains `model = "claude-opus-4-8"`
- **When:** `clp .model.select`
- **Then:** Exits 0. stdout contains `model.select: claude-opus-4-8`. No file is written (read-only get mode).
- **Exit:** 0
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md), [command/007_model.md](../../../../docs/cli/command/007_model.md)

---

### EC-02: `id::VALUE` present — activates set mode, writes preference

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** `~/.clr/config.toml` absent or does not contain `model`
- **When:** `clp .model.select id::claude-opus-4-8`
- **Then:** Exits 0. `~/.clr/config.toml` contains `model = "claude-opus-4-8"`. stdout contains `model.select: claude-opus-4-8 (pinned)`.
- **Exit:** 0
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md), [command/007_model.md](../../../../docs/cli/command/007_model.md)

---

### EC-03: `id::VALUE reset::1` together — mutually exclusive, rejected

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** Any `~/.clr/config.toml` state
- **When:** `clp .model.select id::claude-opus-4-8 reset::1`
- **Then:** Exits 1. stderr contains `id:: and reset::1 are mutually exclusive`. `~/.clr/config.toml` is unchanged.
- **Exit:** 1
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md), [param/066_reset.md](../../../../docs/cli/param/066_reset.md)

---

### EC-04: `id::` with empty value rejected

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** Any `~/.clr/config.toml` state
- **When:** `clp .model.select id::` (empty value after `::`)
- **Then:** Exits 1. stderr contains `id:: must be a non-empty model ID`. `~/.clr/config.toml` is unchanged.
- **Exit:** 1
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md)

---

### EC-05: Minimum boundary — single-character value accepted

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** `~/.clr/config.toml` absent
- **When:** `clp .model.select id::x`
- **Then:** Exits 0. `~/.clr/config.toml` contains `model = "x"` written verbatim — non-empty is the only validation, no minimum length beyond one character.
- **Exit:** 0
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md)

---

### EC-06: Maximum boundary — long realistic model ID accepted

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** `~/.clr/config.toml` absent
- **When:** `clp .model.select id::claude-haiku-4-5-20251001`
- **Then:** Exits 0. `~/.clr/config.toml` contains `model = "claude-haiku-4-5-20251001"` written verbatim, no truncation or reformatting.
- **Exit:** 0
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md)

---

### EC-07: No live API validation at pin time

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** `~/.clr/config.toml` absent. No network access assumed.
- **When:** `clp .model.select id::not-a-real-model-xyz`
- **Then:** Exits 0. `~/.clr/config.toml` contains `model = "not-a-real-model-xyz"`. Value is accepted purely on the non-empty check — invalid IDs are only rejected later by the Claude API when `clr` invokes the subprocess, not at pin time.
- **Exit:** 0
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md)

---

### EC-08: Overwrites an existing pinned value

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** `~/.clr/config.toml` contains `model = "claude-sonnet-5"`
- **When:** `clp .model.select id::claude-opus-4-8`
- **Then:** Exits 0. `~/.clr/config.toml` contains `model = "claude-opus-4-8"`. Prior value overwritten.
- **Exit:** 0
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md)

---

### EC-09: Preserves other keys in `config.toml`

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** `~/.clr/config.toml` contains `model = "claude-sonnet-5"` and `other_key = "unrelated_value"`
- **When:** `clp .model.select id::claude-opus-4-8`
- **Then:** Exits 0. `~/.clr/config.toml` contains both `model = "claude-opus-4-8"` and `other_key = "unrelated_value"` — unrelated keys preserved verbatim.
- **Exit:** 0
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md)

---

### EC-10: Creates `~/.clr/` directory and `config.toml` when absent

> Historical only — describes `.model.select`'s retired `id::` behavior. See the Narrowed-scope note at the top of this file for `id::`'s current coverage on `.provider.select`.

- **Given:** `~/.clr/` directory does not exist on disk.
- **When:** `clp .model.select id::claude-opus-4-8`
- **Then:** Exits 0. `~/.clr/` directory is created. `~/.clr/config.toml` is created and contains `model = "claude-opus-4-8"`.
- **Exit:** 0
- **Source:** [param/064_id.md](../../../../docs/cli/param/064_id.md), [command/007_model.md](../../../../docs/cli/command/007_model.md)
