# tests/

Integration tests for the `assistant_kit` facade crate.

## Responsibility Table

| File | Responsibility |
|------|----------------|
| `integration/` | Compile-check smoke tests — one per feature module (profile, runner, version, assets, storage) |
