#!/usr/bin/env bash
# claude_storage Docker test runner — delegates to universal run/docker-run.
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
RUNNER="$( cd "$SCRIPT_DIR/../../../run" && pwd )/docker-run"
exec "$RUNNER" "$SCRIPT_DIR/runbox.yml" "$@"
