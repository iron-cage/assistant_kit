# Command Tests

Integration test cases for each `claude_storage` CLI command.

## Responsibility Table

| File | Responsibility |
|------|----------------|
| `status.md` | Integration tests for `.status` command |
| `list.md` | Integration tests for `.list` command |
| `show.md` | Integration tests for `.show` command |
| `count.md` | Integration tests for `.count` command |
| `search.md` | Integration tests for `.search` command |
| `export.md` | Integration tests for `.export` command |
| `projects.md` | Integration tests for `.projects` command |
| `path.md` | Integration tests for `.path` command |
| `exists.md` | Integration tests for `.exists` command |
| `session_dir.md` | Integration tests for `.session.dir` command |
| `session_ensure.md` | Integration tests for `.session.ensure` command |
