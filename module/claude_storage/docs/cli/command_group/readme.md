# Command Groups

### Scope

- **Purpose**: Formalize sets of commands that share one implementing function and one parameter set, differing only in default values.
- **Responsibility**: Define command_group membership under a strict identity test — same routine function, same parameter set — distinct from the looser per-parameter cross-command groupings in `param_group/`.
- **In Scope**: Group membership, the Representation Absorption Test applied per candidate pair, shared-routine citations, default divergence (when any exists), and cross-references to commands/parameters/tests/user stories.
- **Out of Scope**: Individual parameter semantics (-> `../param/`), per-parameter cross-command groupings that don't require an identical full parameter set or a shared routine (-> `../param_group/`).

Every command belongs to exactly one group — including a command with no sibling, which still opens its own Singleton Group and still receives a row below; no command is ever left out of **All Groups**. Before adding a new command, this same evaluation also asks whether an existing group's handler could absorb it via a new parameter instead of a new dispatch function. (Per this workspace-group's shared rulebook, CLI Documentation: Command Group Total Partition and Command Group Minimization.)

Every command in `command/` is evaluated against every other command using the Representation Absorption Test before a new command name is ever added — this is a mandatory design gate, not documentation-after-the-fact. A proposed new command that passes the test is a pre-configured alias of an existing command's routine, not a new command_group member requiring its own dispatch registration.

**Representation Absorption Test:** "Would the proposed new command be achievable by changing default values of an existing command's parameters?" A candidate pair qualifies as a `command_group` only when BOTH hold:

1. **Identical routine function** — both commands are registered in `src/cli_main.rs`'s `routines` phf map against the literal same function, or one's routine is a thin, unconditional delegate that calls the other's routine directly (not a shared lower-level helper).
2. **Identical CLI-facing parameter set** — every parameter accepted by one is accepted by the other, differing at most in default values.

A shared private helper function (e.g. `resolve_cmd_path`, `create_storage`) does NOT satisfy criterion 1 — that is ordinary code reuse, not command identity. See Evaluated, Not Qualifying below.

### Responsibility Table

| File | Responsibility |
|------|----------------|
| `readme.md` | This index — Representation Absorption Test, evaluation results, navigation |

### All Groups (12 total)

| # | Group | Members | Shared Handler | Divergence |
|---|-------|---------|-----------------|------------|
| 1 | `.status` | 1 | `status_routine()` (`src/cli/status.rs:19`) | N/A — sole member |
| 2 | `.list` | 1 | `list_routine()` (`src/cli/list.rs:43`) | N/A — sole member |
| 3 | `.show` | 1 | `show_routine()` (`src/cli/show.rs:32`) | N/A — sole member |
| 4 | `.count` | 1 | `count_routine()` (`src/cli/count.rs:24`) | N/A — sole member |
| 5 | `.search` | 1 | `search_routine()` (`src/cli/search.rs:20`) | N/A — sole member |
| 6 | `.export` | 1 | `export_routine()` (`src/cli/export.rs:16`) | N/A — sole member |
| 7 | `.projects` | 1 | `projects_routine()` (`src/cli/projects.rs:696`) | N/A — sole member |
| 8 | `.project.path` | 1 | `project_path_routine()` (`src/cli/session.rs:69`) | N/A — sole member |
| 9 | `.project.exists` | 1 | `project_exists_routine()` (`src/cli/session.rs:105`) | N/A — sole member |
| 10 | `.session.dir` | 1 | `session_dir_routine()` (`src/cli/session.rs:176`) | N/A — sole member |
| 11 | `.session.ensure` | 1 | `session_ensure_routine()` (`src/cli/session.rs:202`) | N/A — sole member |
| 12 | `.tail` | 1 | `tail_routine()` (`src/cli/tail.rs:32`) | N/A — sole member |

**Total:** 12 groups for 12 commands (12 singletons) — a 1:1 groups-to-commands ratio, the maximum possible under Command Group Minimization; no pair in the crate passes the Representation Absorption Test, so no merge opportunity currently exists. Verification method: all 12 `claude_storage` routine functions were enumerated from their `pub fn` definitions — `status_routine()` (`src/cli/status.rs:19`), `list_routine()` (`src/cli/list.rs:43`), `show_routine()` (`src/cli/show.rs:32`), `tail_routine()` (`src/cli/tail.rs:32`), `count_routine()` (`src/cli/count.rs:24`), `search_routine()` (`src/cli/search.rs:20`), `export_routine()` (`src/cli/export.rs:16`), `projects_routine()` (`src/cli/projects.rs:696`), `project_path_routine()` (`src/cli/session.rs:69`), `project_exists_routine()` (`src/cli/session.rs:105`), `session_dir_routine()` (`src/cli/session.rs:176`), `session_ensure_routine()` (`src/cli/session.rs:202`) — then a full pairwise sweep was run against every one of them: `grep -rn '\b<fn>\b' src/ | grep -v 'pub fn <fn>'` for each of the 12 names, covering all 66 unordered pairs. The "zero cross-calls" claim in this section is backed directly by that sweep's raw output, not informal reasoning: every non-definition match across all 12 greps resolved to one of exactly three categories — (a) `src/cli_main.rs`'s `routines` phf map registration (`cli::<fn>,` — a dispatch-table entry, not a call), (b) `src/cli/mod.rs`'s `pub use` re-export list, or (c) a bug-fix source doc comment mentioning another routine's name in prose (e.g. `count.rs:159`'s "Root cause: `count_routine` used exact equality only..." mentioning `show_routine`) — never an actual `fn_name(args)` invocation from within another routine's body. Since criterion 1 (identical routine function) fails for every one of the 66 candidate pairs, every command opens its own Singleton Group. This is a valid, complete outcome, not an incomplete audit — see `01_run_ask.md`-equivalent worked example in the sibling `claude_runner` crate for what a qualifying multi-member group looks like when the underlying dispatch mechanism does share handlers.

### Evaluated, Not Qualifying

Every near-miss candidate found across two passes — (1) the private-helper-sharing candidates identified by the pairwise sweep above, and (2) prose signals found by re-reading every command doc end-to-end for "shares," "matches," "mirrors," "like," "consistent with," and "distinct from" language, each individually verified against source:

| Candidate Pair | Shared Parameter Set | Shared Implementation | Why Not a Command Group |
|-----------------|----------------------|-------------------------|---------------------------|
| `.project.path` / `.project.exists` | `{path::, topic::}` (identical) | `resolve_cmd_path()` (`src/cli/session.rs:15`), `validate_topic()` (`src/cli/session.rs:38`) — private helpers, not the dispatch routine | Distinct routine functions (`project_path_routine()` at `src/cli/session.rs:69` vs `project_exists_routine()` at `src/cli/session.rs:105`) implementing semantically different operations — one computes a path unconditionally (exit 0 always), the other checks existence and diverges exit code (0 vs 1) and stdout/stderr content based on filesystem state. Fails criterion 1 (identical routine) even though criterion 2 (identical parameter set) holds. |
| `.project.path` / `.session.dir` | `{path::, topic::}` (identical) | `resolve_cmd_path()` (`src/cli/session.rs:15`) — private helper only | Distinct routine functions (`project_path_routine()` at `src/cli/session.rs:69` vs `session_dir_routine()` at `src/cli/session.rs:176`) computing different output paths — `.project.path` returns the Claude storage path (`~/.claude/projects/{encoded}/`) via `claude_storage_core::continuation::to_storage_path_for` (called at `src/cli/session.rs:84`); `.session.dir` returns the session working directory (`{base}/-{topic}`) via `resolve_session_dir()` (`src/cli/session.rs:149`, called at `src/cli/session.rs:179`). Same parameter shape, unrelated output semantics. Fails criterion 1. |
| `.project.exists` / `.session.dir` | `{path::, topic::}` (identical) | `resolve_cmd_path()` (`src/cli/session.rs:15`) — private helper only | Distinct routine functions (`project_exists_routine()` at `src/cli/session.rs:105` vs `session_dir_routine()` at `src/cli/session.rs:176`) with unrelated purposes (boolean history check via `claude_storage_core::continuation::check_continuation`, called at `src/cli/session.rs:120`, vs. path computation). Fails criterion 1. |
| `.session.dir` / `.session.ensure` | `{path::, topic::}` vs `{path::, topic::, strategy::}` — **not identical**, `.session.ensure` adds `strategy::` with no equivalent on `.session.dir` | `resolve_session_dir()` (`src/cli/session.rs:149`) — a private helper strictly more specific than the `resolve_cmd_path()` shared by the other three pairs above; called directly by both `session_dir_routine()` (`src/cli/session.rs:179`) and `session_ensure_routine()` (`src/cli/session.rs:205`) | The tightest near-miss in the crate — same private helper AND a documented "use `.session.ensure` to create the directory" pointer in `10_session_dir.md:51` — but still fails both criteria: distinct routine functions (`session_dir_routine()` vs `session_ensure_routine()`, `src/cli/session.rs:176` and `:202`), and `.session.ensure`'s extra `strategy::` parameter (`src/cli/session.rs:207-222`) plus its side effect (`std::fs::create_dir_all`, `src/cli/session.rs:224`) break parameter-set identity. `.session.dir` is read-only; `.session.ensure` is the crate's only mutating command (per `command/readme.md` line 10). |
| `.count` / `.search` / `.export` | Not identical — `.count` `{path, project, session, target}`, `.search` `{case_sensitive, entry_type, project, query, session}`, `.export` `{format, output, project, session_id}` | `load_project_for_param()` (`src/cli/storage.rs:270`) — private helper only, called at `src/cli/count.rs:112,149,193`, `src/cli/search.rs:91,131`, `src/cli/export.rs:46` | Fails both criteria: distinct routine functions (`count_routine()` at `src/cli/count.rs:24`, `search_routine()` at `src/cli/search.rs:20`, `export_routine()` at `src/cli/export.rs:16`) AND non-identical parameter sets. Their source doc comments repeatedly note a shared historical bug pattern (`count.rs:144`: "Commands .count/.search/.export shared this bug...") and cross-reference each other by routine name for partial-UUID prefix matching (`count.rs:156-159`: "consistent with `show_routine` and `export_routine`... `count_routine` used exact equality only"; `search.rs:102-105`: "consistent with `show_routine` and `export_routine`... `search_routine` used exact equality only") — this documents shared bug provenance and independently-applied-but-parallel fixes in a common helper, not command identity; do not misread it as a shared-handler claim. |
| `.status` / `.list` | Not identical — `.status` `{path::, show_tokens::}` (2 params), `.list` `{type::, path::, show_sessions::, session::, agent::, min_entries::, scope::, project::, count::}` (9 params) | `resolve_path_parameter()` (`src/cli/storage.rs:57`) — private helper only, called at `src/cli/status.rs:34` and `src/cli/list.rs:121` | `status_routine()`'s own `Fix(issue-014)` doc comment (`src/cli/status.rs:25-29`) explicitly states the historical bug: "`status_routine` passed path directly to `Storage::with_root()` without resolving special markers..., **unlike `list_routine` which uses `resolve_path_parameter()`**" — naming `list_routine` by identifier as the pre-existing correct-behavior reference the `.status` fix was brought into alignment with. Verified: neither routine calls the other (pairwise sweep, both directions); both now independently call the same private helper `resolve_path_parameter()` post-fix. Distinct routine functions (`status_routine()` at `src/cli/status.rs:19`, `list_routine()` at `src/cli/list.rs:43`) and non-identical parameter sets (9 vs 2 params, `.list` alone has `type::`/`show_sessions::`/`session::`/`agent::`/`min_entries::`/`scope::`/`project::`/`count::`). Fails both criteria. This cross-reference exists only in source comments, not in either command's own doc file (`01_status.md`/`02_list.md` contain no such prose) — no `Referenced Command Group` doc section was added per the step-4 scoping rule (only doc files whose own prose invites the misreading get that section). |
| `.count` / `.show` | Not identical — `.count` `{path, project, session, target}` (uses `session::`), `.show` `{path, project, scope, session_id, show_entries, show_metadata, show_stat, show_tokens}` (uses `session_id::`) | `create_storage()` (`src/cli/storage.rs:22`) — private factory helper only, called at `src/cli/count.rs:49` and (via internal helpers) `src/cli/show.rs:145,183,207,223` | `count_routine()`'s own doc comment (`src/cli/count.rs:12-13`) states its zero-parameter default was deliberately engineered to match `.show`'s CWD-detection default "for UX consistency," and the `Fix(issue-003a)` comment at `count.rs:27-34` is titled "Make .count context-aware **like** .show." `04_count.md:38` documents this as "matches `.show` default." This is a genuine behavioral-parity design decision (both commands independently call `storage.load_project_for_cwd()` when invoked bare) verified against source, but it is parity of *default dispatch outcome*, not shared code — `count_routine()` (`src/cli/count.rs:24`) and `show_routine()` (`src/cli/show.rs:32`) never call each other (confirmed by the pairwise sweep) and their parameter sets differ (`target::`/`session::` vs `session_id::`/`show_entries::`/etc.). Fails both criteria. |
| `.tail` / `.status` | `{path::}` overlaps; `.tail` adds `{tail::, topic::}`, `.status` adds `{show_tokens::}` — **not identical** | No shared private helper; independently duplicated `std::process::exit(2)` bypass pattern | `tail_routine()`'s own doc comment (`src/cli/tail.rs:23-24`) states its exit-2 "no project found" bypass "matches the `.status` command's 'not found = usage error' convention (see `status.rs`)." Verified: `status_routine()` calls `std::process::exit(2)` at `src/cli/status.rs:45`; `tail_routine()` independently calls `std::process::exit(2)` at `src/cli/tail.rs:61` and `:71` — these are two separately-written call sites in different files, not a shared function. `.tail` and `.status` are also the *only* two routines in the crate using this direct-exit bypass (confirmed: `grep -n "std::process::exit" src/cli/*.rs` matches only `status.rs:45`, `tail.rs:61`, `tail.rs:71`). A real convention-parity relationship, verified precisely as `.tail`↔`.status` — note `12_tail.md`'s own Notes section attributes this convention to `.show` instead, which does not match either the source comment or this verification (`show_routine()` never calls `std::process::exit(2)`; see `12_tail.md`'s `### Referenced Command Group` section for the correction) — but zero shared routine and non-identical parameter sets either way. Fails both criteria. |

No other pair among the remaining candidate pairs (66 total minus the 8 listed above) shares even a parameter-set match or a documented behavioral-parity claim; see `command/readme.md`'s Commands Table for each command's declared parameter count.

### Navigation

*(none — all 12 groups are singletons with no multi-member equivalence claim requiring a per-group detail file; see Evaluated, Not Qualifying above for the nearest misses)*
