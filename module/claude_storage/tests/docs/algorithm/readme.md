# Algorithm Doc Entity

### Scope

- **Purpose**: Document test plans for algorithm-level behaviors.
- **Responsibility**: Index of per-algorithm test case planning files.
- **In Scope**: All algorithm instances in `docs/algorithm/`.
- **Out of Scope**: Automated test implementations (→ `tests/` in crate), CLI command specs (→ `docs/cli/command/`).

Direct contract verification for the behaviors documented in `docs/algorithm/`.

### Responsibility Table

| Name | Purpose | Status |
|------|---------|--------|
| `001_agent_session_tracking.md` | AL- test cases for agent session tracking algorithm (`docs/algorithm/001_agent_session_tracking.md`) | ✅ |
