# run.d/

Layer scripts for the `run` verb dispatcher.

| File | Responsibility |
|------|----------------|
| `l1` | Direct execution: `cargo run -p claude_journal_viewer --bin clj`; default when no `VERB_LAYER` set. |
