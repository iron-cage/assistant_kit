#!/usr/bin/env bash
# install — install binary clj to ~/.cargo/bin (cargo ecosystem)
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR/.."
if [[ "${1:-}" == "--dry-run" ]]; then echo "cargo install --path ."; exit 0; fi
exec cargo install --path .
