# tests/

### Scope

**Responsibilities:** Automated integration tests for the `claude_journal_viewer` crate — the `clj` binary (`.list`/`.tail`/`.stats`/`.search`/`.serve`/`.prune`/`.status`/`.export`/`.chart`) and its optional `routines` feature (`register_commands()` for `ast .journal.*` unilang integration).
**In Scope:** All crate functionality exercised via the compiled `clj` binary and the public library API.
**Out of Scope:** Manual testing, test planning documents (→ `docs/`).

### Domain Map

| Domain | File | Tests What |
|--------|------|------------|
| Library API (TC-001) | `lib_test.rs` | `register_commands()` is callable and leaves the registry unchanged |
| `clj` binary (EC-1–EC-38) | `viewer_integration_test.rs` | `.list` table/JSON output and `type`/`since` validation, `.stats by::model` aggregation and `by::dir`/`by::agent` count-ranked grouping with `(no dir)`/`(no agent)` buckets and invalid-`by` validation (EC-21–EC-24, task 543), `.search pattern::` filtering, `.prune dry_run::1` and filename-date prune semantics (old dated file deleted; non-date `.jsonl` and today's file survive), `.status` health report, `.export format::json`, parse-time type validation, `NO_COLOR=1` ANSI suppression, `.tail` blocking behavior, `.chart` rendering, empty-`HOME` journal resolution (EC-25), and parameter-name semantics (EC-26–EC-29): `exit::` and `dir::` filter what their docs say they filter, an unrecognised key exits 1 instead of being ignored — including every *retracted* one, which EC-28 pins per-command: `.list wide::`/`columns::`, `.stats verbosity::`, `.search include_stdout::`, and `.tail since::`/`limit::` (the last two bounded on wall-clock time, since a regressed rejection would make `.tail` follow forever and hang the suite rather than fail it) — and `no_color::1` matches the env var; and `.list sort::`/`reverse::`/`limit::` ordering plus `.tail format::` rendering (EC-30–EC-34): every documented sort field orders correctly in both directions against a fixture whose append order matches none of them, bad `sort::`/`reverse::` values exit 1, `limit` caps after the sort with `limit::0` meaning unlimited, `.list`'s non-table formats are byte-identical to `.export`'s, and `.tail` renders each format with a bad one rejected before the follow loop blocks; and `.status verbosity::` detail levels (EC-35): level 0 collapses to one line, level 2 appends a per-file breakdown whose rows carry their own sizes, a value above the documented range clamps to level 2, and a non-integer value exits 1; and the two per-*type* contracts (EC-36–EC-37), which walk the Referenced Parameters tables of `type/08_boolean.md` and `type/04_integer.md` rather than naming sites individually — every `Boolean` parameter takes `0`/`1` and exits 1 on `true`/`false`/`yes`/`2`/`-1`/empty, and every `Integer` parameter rejects negative, non-numeric and out-of-range values, with `exit::255` accepted and `exit::256` refused; and `.search`'s field set (EC-38): the prompt (`message`) is matched, `dir` is not, pinned by a fixture carrying one phrase in both fields so a single `1 match` assertion fails at 0 if `message` goes unread and at 2 if the set widens |
| CLI surface consistency (DC-1–DC-5) | `cli_doc_consistency.rs` | That the four descriptions of which command takes which parameter agree. Every parameter a command page names has a page of its own (DC-1) and every live page is named by at least one command page (DC-2); `param/readme.md`'s `Commands` column and each page's Referenced Commands table are equal in both directions (DC-3); each page's command set equals the binary's, read by probing `clj <command> zz_probe_unknown_param::1` and parsing the `Accepted:` line rather than transcribing `known_params` (DC-4); and `param/readme.md`'s two declared totals equal the live page count (DC-5). A page is exempt from DC-2 and DC-5 when its own `- **Type:**` line says `not accepted`, so retracting a parameter needs no test edit. Enforces [invariant/003_cli_surface_consistency.md](../docs/invariant/003_cli_surface_consistency.md) |
| `.serve` HTTP (FT-1–FT-14, IT-4, TC-3, IN-1–IN-3) | `serve_test.rs` | Startup line and loopback default, `GET /` dashboard, `/api/events` with query filtering and 400 on bad values or unrecognised keys, `/api/stats` grouping and 400 on bad `by`, `/api/health` structure and empty-journal nulls, 404 JSON for unknown `/api/*`, `port::`/`refresh::`/`open::` handling, CDN-free HTML, SIGTERM termination (FT-8) and graceful SIGTERM/SIGINT shutdown of an idle server with exit 0 (FT-13), `bind::` interface selection with its exposure warning, exit-1 bind failures on a busy port or unparseable address, and pre-bind parameter validation (FT-14): a bad `port::` or `open::` exits 1 before the socket is opened, while `port::65535` reaches the bind attempt intact |
