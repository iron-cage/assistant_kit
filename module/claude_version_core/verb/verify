#!/usr/bin/env bash
# do verify — full checks: tests, lints, dependency analysis, audit (cargo ecosystem)
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR/.."
if [[ "${1:-}" == "--dry-run" ]]; then echo "w3 .test level::4"; exit 0; fi
exec w3 .test level::4
