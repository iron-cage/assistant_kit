# claude_memory

Reserved skeleton — not a workspace member. There is no `Cargo.toml`, no source,
and no tests yet; every subdirectory below is an empty placeholder holding only its
own stub `readme.md`. The directory exists to reserve the name and shape, so its
contents are described here as reserved rather than as implemented behavior.

### Responsibility Table

| File | Responsibility |
|------|----------------|
| `src/` | Reserved for crate implementation — empty placeholder. |
| `tests/` | Reserved for the test suite — empty placeholder. |
| `docs/` | Reserved for behavioral requirements — empty placeholder. |
| `sh/` | Reserved for shell helper scripts — empty placeholder. |
