[package]
name = "json_redact"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
readme = "readme.md"
repository.workspace = true
homepage.workspace = true
description = "Domain-agnostic redaction of sensitive values from strings and JSON"
keywords = [ "redact", "json", "secrets", "sanitize" ]
categories = [ "development-tools" ]

[lints]
workspace = true

[lib]
name = "json_redact"
path = "src/lib.rs"

[dependencies]
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
