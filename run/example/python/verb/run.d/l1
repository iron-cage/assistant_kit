#!/usr/bin/env bash
# run.d/l1 — execute entry point via python directly (container execution layer).
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec "$SCRIPT_DIR/../../.venv/bin/python" -m example_lib
