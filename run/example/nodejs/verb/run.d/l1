#!/usr/bin/env bash
# run.d/l1 — execute entry point via node directly (container execution layer).
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec node "$SCRIPT_DIR/../../src/main.js"
