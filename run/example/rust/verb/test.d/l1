#!/usr/bin/env bash
# l1 — bare test execution; runs inside the container (VERB_LAYER=l1 set by runbox-run).
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec cargo test --manifest-path "$SCRIPT_DIR/../../Cargo.toml"
