#!/usr/bin/env bash
# workspace Docker test runner — delegates to universal run/docker-run.
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec "$SCRIPT_DIR/docker-run" "$SCRIPT_DIR/docker.yml" "$@"
