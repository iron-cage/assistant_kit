#!/usr/bin/env bash
# do build — compile all workspace crates
set -euo pipefail
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR/.."
if [[ "${1:-}" == "--dry-run" ]]; then echo "cargo build --workspace"; exit 0; fi
exec cargo build --workspace
